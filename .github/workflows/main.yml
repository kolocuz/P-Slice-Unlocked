name: Build Windows, Linux and Android

on:
  workflow_dispatch:

jobs:
  BuildDesktopCache:
    name: Build Desktop Cache
    permissions: write-all
    uses: ./.github/workflows/dependencies.yml
    with:
      os: ubuntu-24.04
      setupScript: sh ./setup/unix.sh
      CACHE_NAME: P-Slice.1.0.desktop-cache

  BuildMobileCache:
    name: Build Mobile Cache
    permissions: write-all
    uses: ./.github/workflows/dependencies.yml
    with:
      os: ubuntu-24.04
      setupScript: sh ./setup/mobile.sh
      CACHE_NAME: P-Slice.1.0.mobile-cache

  BuildDesktop:
    name: Build Desktop
    needs: BuildDesktopCache
    permissions: write-all
    strategy:
      matrix:
        include:
          - name: Windows
            os: windows-2022
            buildArgs: "windows -final"
            exportPath: windows
            artifactName: PSlice-Windows
            cacheLibrariesPath: P-Slice.1.0.desktop-cache
            cachePath: P-Slice.1.0.build-windows
            artifactPath: export\release\windows\bin\*
          - name: Linux
            os: ubuntu-22.04
            buildArgs: "linux -final"
            exportPath: linux
            artifactName: PSlice-Linux
            cachePath: P-Slice.1.0.build-linux
            cacheLibrariesPath: P-Slice.1.0.desktop-cache
            artifactPath: export/release/linux/bin/*
    uses: ./.github/workflows/build.yml
    with:
      name: ${{ matrix.name }}
      os: ${{ matrix.os }}
      buildArgs: ${{ matrix.buildArgs }}
      cacheLibrariesPath: ${{ matrix.cacheLibrariesPath }}
      exportPath: ${{ matrix.exportPath }}
      artifactName: ${{ matrix.artifactName }}
      artifactPath: ${{ matrix.artifactPath }}
      cachePath: ${{ matrix.cachePath }}

  BuildAndroid:
    name: Build Android
    needs: BuildMobileCache
    permissions: write-all
    uses: ./.github/workflows/build.yml
    secrets: inherit
    with:
      name: Android
      os: ubuntu-24.04
      buildArgs: "android"
      exportPath: android
      cacheLibrariesPath: P-Slice.1.0.mobile-cache
      cachePath: P-Slice.1.0.build-android
      artifactName: PSlice-Android
      artifactPath: "export/release/android/bin/app/build/outputs/apk/release/*.apk"
